# Project Instructions

- Fix the general mechanism behind a failure, not a particular person, nickname, message, or incident.
- Maintain this personal project's current implementation. Do not add backward compatibility or migration frameworks without an explicit need; keep one-off data conversions out of normal startup.
- Keep tests focused on meaningful behavior and concrete risks, not case counts, incidental wording, or repeated coverage across layers.
- When pushing, push the same branch and commits to both `origin` (GitHub) and `gitea`, and verify both results.
